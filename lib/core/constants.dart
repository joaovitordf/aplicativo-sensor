import 'package:flutter/material.dart';

const String logoSensor = 'assets/logoSensor.png';
const String logoMini = 'assets/logoMini.png';

/// Brand Blue used throughout the UI
const Color kBrandBlue = Color(0xFF089bfe);

/// Brand Blue with 10% opacity, used for icon container backgrounds
const Color kBrandBlueLight = Color.fromRGBO(8, 155, 254, 0.1);

// ─── UI Palette ─────────────────────────────────────────────────────────────

/// Dark teal – primary dark accent
const Color kPalettePrimaryDark = Color(0xFF016895);

/// Accent blue – main interactive blue (same as kBrandBlue)
const Color kPaletteAccentBlue = Color(0xFF089bfe);

/// Light gray – borders, dividers, disabled states
const Color kPaletteLightGray = Color(0xFFD6D6D6);

/// Light blue – highlights and secondary elements
const Color kPaletteLightBlue = Color(0xFF8DD0FA);

/// Deep blue – top bar background, primary navigation chrome
const Color kPaletteDeepBlue = Color(0xFF0D5EA8);

/// Teal – success states and secondary accents
const Color kPaletteTeal = Color(0xFF37B5AD);
